#include "lists.h"
/**
 * pop_listint - delete the first node of the list and return data
 * @head: pointer to the list
 * Return:int
 */
int pop_listint(listint_t **head);
{
	int n;
	listint_t *tmp;
}
